# n8n-workflow-generator
Claude skill that generates valid, importable n8n workflow JSON from a plain-English automation description — verified node reference, real JSON schema, and a structural validator, not just static templates.
